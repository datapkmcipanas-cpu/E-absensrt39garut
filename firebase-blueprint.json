{
  "entities": {
    "User": {
      "title": "User",
      "description": "Employee information",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Firebase Auth UID" },
        "displayName": { "type": "string", "description": "Full name" },
        "email": { "type": "string", "format": "email", "description": "Email address" },
        "role": { "type": "string", "enum": ["admin", "employee"], "description": "User role" },
        "nip": { "type": "string", "description": "Nomor Induk Pegawai" },
        "position": { "type": "string", "description": "Jabatan/Posisi" }
      },
      "required": ["uid", "displayName", "email", "role"]
    },
    "Attendance": {
      "title": "Attendance",
      "description": "Daily attendance record with photo or file proof",
      "type": "object",
      "properties": {
        "userId": { "type": "string", "description": "Reference to User uid" },
        "userName": { "type": "string", "description": "Full name of employee" },
        "nip": { "type": "string", "description": "Employee NIP" },
        "position": { "type": "string", "description": "Employee Position" },
        "timestamp": { "type": "string", "format": "date-time", "description": "Time of action" },
        "type": { "type": "string", "enum": ["clock-in", "clock-out", "izin", "sakit", "dinas-luar"], "description": "Type of attendance" },
        "location": {
          "type": "object",
          "properties": {
            "latitude": { "type": "number" },
            "longitude": { "type": "number" }
          }
        },
        "photoUrl": { "type": "string", "description": "Photo or file proof (base64)" },
        "notes": { "type": "string", "description": "Optional notes for permits" }
      },
      "required": ["userId", "userName", "timestamp", "type", "location"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "User profile documents"
    },
    "/attendance/{attendanceId}": {
      "schema": "Attendance",
      "description": "Attendance records"
    }
  }
}
